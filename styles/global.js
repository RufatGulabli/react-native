import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 20
    },
    titleText: {
        fontFamily: 'nunito-regular',
        fontSize: 28
    }
});